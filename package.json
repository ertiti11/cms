{
  "name": "cms2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"php artisan serve\" \"cd ui && pnpm run dev\"",
    "backend": "php artisan serve",
    "ui": "cd ui && pnpm run dev",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "pocketbase": "^0.21.2"
  },
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
